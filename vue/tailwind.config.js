import Animate from 'tailwindcss-animate'

/** @type {import('tailwindcss').Config} */
export default {
  darkMode: ['class'],
  safelist: ['dark', 'inline-block'],

  theme: {
  },

  plugins: [
    Animate,
  ],
}
